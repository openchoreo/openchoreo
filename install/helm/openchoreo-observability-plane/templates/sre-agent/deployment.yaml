{{- if .Values.rca.enabled }}
{{- if not .Values.rca.secretName }}
{{- fail "rca.secretName is required when rca is enabled. Create a Secret with keys matching the RCA agent's expected env vars (RCA_LLM_API_KEY, OAUTH_CLIENT_SECRET, and optionally SQL_BACKEND_URI), then set rca.secretName." }}
{{- end }}
{{- if not .Values.rca.observerApiUrl }}
{{- fail "rca.observerApiUrl is required when rca is enabled. Set it to the Observer API base URL (e.g. http://observer:8080)." }}
{{- end }}
{{- if and (eq .Values.rca.reportBackend "sqlite") (gt (.Values.rca.replicas | int) 1) }}
{{- fail "rca.replicas must be 1 when using sqlite backend (ReadWriteOnce PVC). Set rca.replicas=1 or use reportBackend=postgresql." }}
{{- end }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.rca.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "openchoreo-observability-plane.componentLabels" (dict "context" . "component" .Values.rca.name) | nindent 4 }}
spec:
  replicas: {{ .Values.rca.replicas }}
  strategy:
    type: {{ ternary "Recreate" "RollingUpdate" (eq (.Values.rca.reportBackend | default "sqlite") "sqlite") }}
  selector:
    matchLabels:
      {{- include "openchoreo-observability-plane.componentSelectorLabels" (dict "context" . "component" .Values.rca.name) | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/sre-agent/rca-agent-config.yaml") . | sha256sum }}
        checksum/observer-auth-config: {{ include (print $.Template.BasePath "/observer/observer-auth-configmap.yaml") . | sha256sum }}
      labels:
        {{- include "openchoreo-observability-plane.componentSelectorLabels" (dict "context" . "component" .Values.rca.name) | nindent 8 }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.rca.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.rca.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.rca.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.rca.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 12000
        runAsGroup: 12000
        fsGroup: 12000
      {{- with .Values.rca.serviceAccountName }}
      serviceAccountName: {{ . }}
      {{- end }}
      containers:
      - name: {{ .Values.rca.name }}
        image: "{{ include "openchoreo-observability-plane.image" (dict "context" . "image" .Values.rca.image) }}"
        imagePullPolicy: {{ .Values.rca.image.pullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.rca.service.port }}
          protocol: TCP
        envFrom:
        - secretRef:
            name: {{ .Values.rca.secretName }}
        - configMapRef:
            name: rca-agent-config
        {{- if .Values.rca.extraEnvs }}
        env:
        {{- toYaml .Values.rca.extraEnvs | nindent 8 }}
        {{- end }}
        resources:
          {{- toYaml .Values.rca.resources | nindent 10 }}

        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3

        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3

        volumeMounts:
        - name: auth-config
          mountPath: /etc/openchoreo
          readOnly: true
        {{- if eq .Values.rca.reportBackend "sqlite" }}
        - name: data
          mountPath: /app/data
        {{- end }}

        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
          runAsNonRoot: true
          runAsUser: 12000
      # Sidecars adicionales. Existe para el Cloud SQL Auth Proxy: la instancia no es
      # alcanzable por red desde este cluster (el peering de VPC no es transitivo y una
      # instancia admite una sola red privada), asi que el patron soportado por GCP es
      # un proxy local y el workload conectando a 127.0.0.1. Sin esto habia que
      # parchear el Deployment a mano y el proximo `helm upgrade` lo borraba.
      {{- with .Values.rca.extraContainers }}
        {{- toYaml . | nindent 6 }}
      {{- end }}
      volumes:
      - name: auth-config
        configMap:
          name: observer-auth-config
      {{- if eq .Values.rca.reportBackend "sqlite" }}
      - name: data
        persistentVolumeClaim:
          claimName: {{ .Values.rca.name }}-data
      {{- end }}
{{- end }}
      {{- with .Values.rca.extraVolumes }}
        {{- toYaml . | nindent 6 }}
      {{- end }}
